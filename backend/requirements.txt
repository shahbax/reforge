# Core runtime — enough to run the API and the full pipeline in mock mode.
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30
pydantic>=2.7,<3.0
pydantic-settings>=2.3
PyJWT>=2.8
cryptography>=42.0
python-multipart>=0.0.9
httpx>=0.27
psycopg[binary]>=3.2
psycopg-pool>=3.2

# Test
pytest>=8.0
